openapi: 3.1.0
info:
  title: LifeOS API
  version: 0.2.0
  description: |
    REST surface for LifeOS. Auth: JWT Bearer after API key or Telegram Mini App initData.
    Source of truth for route list: `internal/transport/http/api/router.go`.
paths:
  /api/v1/auth/token:
    post:
      summary: Issue JWT by Telegram ID
      parameters:
        - in: header
          name: X-API-Key
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [telegram_id]
              properties:
                telegram_id:
                  type: integer
                  format: int64
      responses:
        '200':
          description: Access token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenResponse'
        '401':
          description: Invalid API key
  /api/v1/auth/telegram-webapp:
    post:
      summary: Issue JWT from Telegram Mini App initData
      description: |
        Validates `window.Telegram.WebApp.initData` using the bot token
        (HMAC-SHA256 per Telegram WebApp docs), upserts the user, and returns a JWT.
        `auth_date` must be within JWT TTL (default 24h).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [init_data]
              properties:
                init_data:
                  type: string
                  description: Raw query-string from Telegram.WebApp.initData
      responses:
        '200':
          description: Access token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenResponse'
        '400':
          description: Missing init_data
        '401':
          description: Invalid or expired init_data
        '503':
          description: Auth or bot token not configured
  /api/v1/tasks/today:
    get:
      summary: List tasks for today
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Task list
  /api/v1/tasks:
    get:
      summary: List open tasks by hashtag
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: tag
          schema:
            type: string
        - in: query
          name: from
          schema:
            type: string
            format: date
          description: With `to`, list open tasks due in range (YYYY-MM-DD)
        - in: query
          name: to
          schema:
            type: string
            format: date
      responses:
        '200':
          description: Task list filtered by tag or due date range
        '400':
          description: Missing tag or invalid date range
    post:
      summary: Create task
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title]
              properties:
                title:
                  type: string
                description:
                  type: string
                priority:
                  type: string
                due_date:
                  type: string
                  format: date
                duration_minutes:
                  type: integer
                  minimum: 1
                tags:
                  type: array
                  items:
                    type: string
                project_ids:
                  type: array
                  items:
                    type: string
                    format: uuid
      responses:
        '201':
          description: Created
  /api/v1/tasks/{id}:
    get:
      summary: Get task by id
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Task
        '404':
          description: Not found
    patch:
      summary: Edit task (partial update)
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                title:
                  type: string
                description:
                  type: string
                  nullable: true
                  description: Explicit null or empty clears description
                clear_description:
                  type: boolean
                priority:
                  type: string
                due_date:
                  type: string
                  format: date
                  nullable: true
                  description: Explicit null or empty clears due_date
                clear_due_date:
                  type: boolean
                duration_minutes:
                  type: integer
                  minimum: 1
                clear_duration:
                  type: boolean
                tags:
                  type: array
                  items:
                    type: string
                project_ids:
                  type: array
                  items:
                    type: string
                    format: uuid
      responses:
        '200':
          description: Updated
        '404':
          description: Not found
    delete:
      summary: Soft-delete task
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Deleted
        '404':
          description: Not found
  /api/v1/tasks/{id}/complete:
    post:
      summary: Complete task
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Completed
  /api/v1/tasks/{id}/reopen:
    post:
      summary: Reopen a done task
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Reopened
        '404':
          description: Not found
  /api/v1/tasks/{id}/cancel:
    post:
      summary: Cancel task
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Cancelled
        '404':
          description: Not found
  /api/v1/tasks/{id}/archive:
    post:
      summary: Archive task (soft cancel)
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Archived
        '404':
          description: Not found
  /api/v1/tasks/{id}/reschedule:
    post:
      summary: Reschedule task to a new due date
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [due_date]
              properties:
                due_date:
                  type: string
                  format: date
      responses:
        '200':
          description: Rescheduled
        '404':
          description: Not found
  /api/v1/projects/progress:
    get:
      summary: Project progress
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: project_id
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Progress
  /api/v1/reviews/morning:
    get:
      summary: Morning review text
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Review
  /api/v1/reviews/evening:
    get:
      summary: Evening review text
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Review
  /api/v1/reviews/weekly:
    get:
      summary: Weekly review text
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Review
  /api/v1/reviews/monthly:
    get:
      summary: Monthly review text
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: previous_month
          schema:
            type: boolean
      responses:
        '200':
          description: Review
  /api/v1/priorities:
    get:
      summary: Top priorities for today
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Priority list
  /api/v1/analytics/summary:
    get:
      summary: Productivity summary for current month
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Summary matching Mini App AnalyticsSummary (percent fields are ints 0–100)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AnalyticsSummary'
  /api/v1/finance/income:
    post:
      summary: Record income
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [amount_cents]
              properties:
                amount_cents:
                  type: integer
                  format: int64
                description:
                  type: string
                currency:
                  type: string
      responses:
        '201':
          description: Recorded
  /api/v1/finance/expense:
    post:
      summary: Record expense
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [amount_cents]
              properties:
                amount_cents:
                  type: integer
                  format: int64
                category:
                  type: string
                currency:
                  type: string
      responses:
        '201':
          description: Recorded
  /api/v1/finance/cash-flow:
    get:
      summary: Monthly cash flow
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Cash flow summary
  /api/v1/finance/overview:
    get:
      summary: Finance overview for a calendar month
      description: |
        Period is `YYYY-MM` interpreted in the user's timezone.
        Categories are expense totals for that month with percent of expense_cents (0–100).
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: period
          required: true
          schema:
            type: string
            pattern: '^\d{4}-\d{2}$'
            example: '2026-07'
          description: Calendar month key (YYYY-MM)
      responses:
        '200':
          description: Finance overview matching Mini App FinanceOverview
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FinanceOverview'
        '400':
          description: Invalid or malformed period
  /api/v1/finance/debts:
    get:
      summary: List open debts
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Debt list
          content:
            application/json:
              schema:
                type: object
                required: [debts]
                properties:
                  debts:
                    type: array
                    items:
                      $ref: '#/components/schemas/Debt'
    post:
      summary: Create debt
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [creditor, amount_cents]
              properties:
                creditor:
                  type: string
                amount_cents:
                  type: integer
                  format: int64
                due_date:
                  type: string
                  format: date
                  description: Optional repayment deadline (YYYY-MM-DD)
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Debt'
        '400':
          description: Invalid creditor, amount, or due_date
  /api/v1/finance/debts/{id}/pay:
    post:
      summary: Pay debt
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [amount_cents]
              properties:
                amount_cents:
                  type: integer
                  format: int64
                regular:
                  type: boolean
                  description: If true, advances next_payment_date for installment plan
      responses:
        '200':
          description: Updated debt
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Debt'
        '400':
          description: Invalid amount, overpayment, or debt not open
        '404':
          description: Debt not found
  /api/v1/finance/plan:
    get:
      summary: Finance plan (planned income/expense + debt installments)
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Planned cashflows and upcoming debt installments
    post:
      summary: Create planned income or expense
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [kind, title, amount_cents]
              properties:
                kind:
                  type: string
                  enum: [income, expense]
                title:
                  type: string
                amount_cents:
                  type: integer
                  format: int64
                interval:
                  type: string
                  enum: [once, weekly, monthly]
                next_date:
                  type: string
                  format: date
      responses:
        '201':
          description: Created planned item
        '400':
          description: Invalid payload
  /api/v1/finance/plan/{id}:
    delete:
      summary: Delete planned cashflow
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Deleted
        '404':
          description: Not found
  /api/v1/finance/plan/{id}/complete:
    post:
      summary: Complete one plan occurrence (advance next_date or delete once)
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Occurrence completed (plan advanced/deleted; income/expense posted when configured)
          content:
            application/json:
              schema:
                type: object
                required: [deleted]
                properties:
                  deleted:
                    type: boolean
                  posted:
                    type: boolean
                  posted_cents:
                    type: integer
                  posted_kind:
                    type: string
                    enum: [income, expense]
                  item:
                    type: object
        '404':
          description: Not found
  /api/v1/habits/today:
    get:
      summary: Habits for today
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Habit list with today completion and streak
          content:
            application/json:
              schema:
                type: object
                required: [habits]
                properties:
                  habits:
                    type: array
                    items:
                      $ref: '#/components/schemas/HabitDay'
  /api/v1/habits:
    post:
      summary: Create habit
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name:
                  type: string
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Habit'
        '400':
          description: Invalid name
  /api/v1/habits/{id}/track:
    post:
      summary: Track habit completion
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Tracked
          content:
            application/json:
              schema:
                type: object
                required: [name, streak]
                properties:
                  name:
                    type: string
                  streak:
                    type: integer
        '400':
          description: Invalid habit id
        '404':
          description: Habit not found
  /api/v1/reminders:
    get:
      summary: List pending reminders
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Reminder list
          content:
            application/json:
              schema:
                type: object
                required: [reminders]
                properties:
                  reminders:
                    type: array
                    items:
                      $ref: '#/components/schemas/Reminder'
    post:
      summary: Schedule reminder
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [message, fire_at]
              properties:
                message:
                  type: string
                fire_at:
                  type: string
                  format: date-time
      responses:
        '201':
          description: Created reminder
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Reminder'
        '400':
          description: Missing fields, invalid fire_at, or fire_at in the past
  /api/v1/reminders/{id}:
    delete:
      summary: Cancel pending reminder
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Cancelled reminder
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Reminder'
        '400':
          description: Invalid reminder id
        '404':
          description: Not found
  /api/v1/notes:
    get:
      summary: List or search notes
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: q
          required: false
          schema:
            type: string
          description: Search notes by body substring (case-insensitive)
        - in: query
          name: tag
          required: false
          schema:
            type: string
          description: Filter notes by tag
      responses:
        '200':
          description: Note list
          content:
            application/json:
              schema:
                type: object
                required: [notes]
                properties:
                  notes:
                    type: array
                    items:
                      $ref: '#/components/schemas/Note'
                  query:
                    type: string
                  tag:
                    type: string
    post:
      summary: Create note
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [body]
              properties:
                body:
                  type: string
                tags:
                  type: array
                  items:
                    type: string
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Note'
        '400':
          description: Body required
  /api/v1/notes/{id}:
    get:
      summary: Get note by id
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Note
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Note'
        '404':
          description: Not found
    patch:
      summary: Update note body
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [body]
              properties:
                body:
                  type: string
      responses:
        '200':
          description: Updated note
        '400':
          description: Body required
        '404':
          description: Not found
    delete:
      summary: Delete note
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Deleted note
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Note'
        '400':
          description: Invalid note id
        '404':
          description: Not found
  /api/v1/career/contacts:
    get:
      summary: List or search career contacts
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: q
          schema:
            type: string
      responses:
        '200':
          description: Contact list
          content:
            application/json:
              schema:
                type: object
                required: [contacts]
                properties:
                  contacts:
                    type: array
                    items:
                      $ref: '#/components/schemas/Contact'
                  query:
                    type: string
    post:
      summary: Create career contact
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name:
                  type: string
                company:
                  type: string
                role:
                  type: string
                notes:
                  type: string
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Contact'
        '400':
          description: Name required
  /api/v1/career/contacts/{id}:
    delete:
      summary: Delete career contact
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Contact'
        '400':
          description: Invalid contact id
        '404':
          description: Not found
  /api/v1/career/skills:
    get:
      summary: List or search career skills
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: q
          schema:
            type: string
      responses:
        '200':
          description: Skill list
          content:
            application/json:
              schema:
                type: object
                required: [skills]
                properties:
                  skills:
                    type: array
                    items:
                      $ref: '#/components/schemas/Skill'
                  query:
                    type: string
    post:
      summary: Create career skill
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name:
                  type: string
                level:
                  type: string
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Skill'
        '400':
          description: Name required
  /api/v1/career/skills/{id}:
    delete:
      summary: Delete career skill
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Skill'
        '400':
          description: Invalid skill id
        '404':
          description: Not found
  /api/v1/health/weight:
    get:
      summary: List recent weight logs
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Weight history
          content:
            application/json:
              schema:
                type: object
                required: [weights]
                properties:
                  weights:
                    type: array
                    items:
                      $ref: '#/components/schemas/WeightLog'
    post:
      summary: Record weight
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [weight_kg]
              properties:
                weight_kg:
                  type: number
                  format: double
      responses:
        '201':
          description: Recorded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WeightLog'
        '400':
          description: weight_kg required
  /api/v1/health/weight/latest:
    get:
      summary: Latest weight entry
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Latest weight
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WeightLog'
        '404':
          description: No weight records (Mini App treats as empty)
  /api/v1/health/steps:
    get:
      summary: List recent step logs
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Steps history
          content:
            application/json:
              schema:
                type: object
                required: [steps]
                properties:
                  steps:
                    type: array
                    items:
                      $ref: '#/components/schemas/StepLog'
    post:
      summary: Record steps
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [steps]
              properties:
                steps:
                  type: integer
                  minimum: 1
                  maximum: 200000
      responses:
        '201':
          description: Recorded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StepLog'
        '400':
          description: steps required
  /api/v1/health/steps/latest:
    get:
      summary: Latest steps entry
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Latest steps
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StepLog'
        '404':
          description: No step records (Mini App treats as empty)
  /api/v1/health/sleep:
    get:
      summary: List recent sleep logs
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Sleep history
          content:
            application/json:
              schema:
                type: object
                required: [sleep]
                properties:
                  sleep:
                    type: array
                    items:
                      $ref: '#/components/schemas/SleepLog'
    post:
      summary: Record sleep
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                duration_hours:
                  type: number
                  format: double
                  minimum: 0
                  exclusiveMinimum: true
                  maximum: 24
                duration_minutes:
                  type: integer
                  minimum: 1
                  maximum: 1440
      responses:
        '201':
          description: Recorded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SleepLog'
        '400':
          description: duration_hours or duration_minutes required
  /api/v1/health/sleep/latest:
    get:
      summary: Latest sleep entry
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Latest sleep
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SleepLog'
        '404':
          description: No sleep records (Mini App treats as empty)
  /api/v1/calendar/today:
    get:
      summary: Events for today
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Event list
          content:
            application/json:
              schema:
                type: object
                required: [events]
                properties:
                  events:
                    type: array
                    items:
                      $ref: '#/components/schemas/CalendarEvent'
  /api/v1/calendar/events:
    post:
      summary: Create calendar event
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title, starts_at]
              properties:
                title:
                  type: string
                starts_at:
                  type: string
                  format: date-time
                  description: RFC3339 timestamp
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CalendarEvent'
        '400':
          description: Invalid title or starts_at
  /api/v1/projects:
    get:
      summary: List active projects
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: sphere_id
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Project list
    post:
      summary: Create project
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, sphere_ids]
              properties:
                name:
                  type: string
                sphere_ids:
                  type: array
                  items:
                    type: string
                    format: uuid
                outcome:
                  type: string
                target_value:
                  type: string
                unit:
                  type: string
                target_date:
                  type: string
                  format: date
      responses:
        '201':
          description: Created
  /api/v1/projects/{id}/tasks:
    get:
      summary: Tasks in project
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Task list
  /api/v1/projects/{id}/archive:
    post:
      summary: Archive project
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Archived
  /api/v1/settings:
    get:
      summary: User settings
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSettings'
  /api/v1/settings/spheres:
    get:
      summary: List life spheres
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Sphere list
          content:
            application/json:
              schema:
                type: object
                required: [spheres]
                properties:
                  spheres:
                    type: array
                    items:
                      $ref: '#/components/schemas/Sphere'
    post:
      summary: Create life sphere
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name:
                  type: string
                sort_order:
                  type: integer
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Sphere'
        '400':
          description: Invalid name
  /api/v1/settings/spheres/{id}:
    put:
      summary: Update life sphere
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, sort_order]
              properties:
                name:
                  type: string
                sort_order:
                  type: integer
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Sphere'
        '400':
          description: Invalid input
        '404':
          description: Not found
    delete:
      summary: Delete life sphere
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Deleted sphere (body returned for Mini App)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Sphere'
        '404':
          description: Not found
        '409':
          description: Sphere has linked projects
  /api/v1/settings/morning-review:
    put:
      summary: Update morning review time
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [hour, minute]
              properties:
                hour:
                  type: integer
                minute:
                  type: integer
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                type: object
                required: [morning_review_at]
                properties:
                  morning_review_at:
                    $ref: '#/components/schemas/TimeOfDay'
        '400':
          description: Invalid time
  /api/v1/settings/evening-review:
    put:
      summary: Update evening review time
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [hour, minute]
              properties:
                hour:
                  type: integer
                minute:
                  type: integer
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                type: object
                required: [evening_review_at]
                properties:
                  evening_review_at:
                    $ref: '#/components/schemas/TimeOfDay'
        '400':
          description: Invalid time
  /api/v1/settings/quiet-hours:
    put:
      summary: Update quiet hours
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [start_hour, start_minute, end_hour, end_minute]
              properties:
                start_hour:
                  type: integer
                start_minute:
                  type: integer
                end_hour:
                  type: integer
                end_minute:
                  type: integer
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                type: object
                required: [quiet_hours_start, quiet_hours_end]
                properties:
                  quiet_hours_start:
                    $ref: '#/components/schemas/TimeOfDay'
                  quiet_hours_end:
                    $ref: '#/components/schemas/TimeOfDay'
        '400':
          description: Invalid time
components:
  schemas:
    FinanceOverview:
      type: object
      required: [period_label, income_cents, expense_cents, net_cents, currency, categories]
      properties:
        period_label:
          type: string
          description: Russian month name and year (e.g. "июль 2026")
          example: июль 2026
        income_cents:
          type: integer
          format: int64
        expense_cents:
          type: integer
          format: int64
        net_cents:
          type: integer
          format: int64
        currency:
          type: string
          example: RUB
        categories:
          type: array
          items:
            $ref: '#/components/schemas/FinanceCategory'
    FinanceCategory:
      type: object
      required: [name, amount_cents, percent]
      properties:
        name:
          type: string
        amount_cents:
          type: integer
          format: int64
        percent:
          type: number
          format: double
          description: Share of expense_cents (0–100)
        color_hint:
          type: string
          description: Optional color hint for UI
    TokenResponse:
      type: object
      required: [access_token, expires_in, token_type]
      properties:
        access_token:
          type: string
        expires_in:
          type: integer
          description: Seconds until JWT expiry
        token_type:
          type: string
          example: Bearer
        telegram_id:
          type: integer
          format: int64
          description: Telegram user id from signed initData.user.id (webapp) or looked-up user (API key)
    Sphere:
      type: object
      required: [id, name, sort_order, created_at]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        sort_order:
          type: integer
        created_at:
          type: string
          format: date-time
    Habit:
      type: object
      required: [id, name, frequency]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        frequency:
          type: string
          example: daily
    HabitDay:
      type: object
      required: [id, name, today_completed, streak]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        today_completed:
          type: boolean
        streak:
          type: integer
    CalendarEvent:
      type: object
      required: [id, title, starts_at]
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
        starts_at:
          type: string
          format: date-time
          description: RFC3339 UTC
    TimeOfDay:
      type: object
      required: [hour, minute]
      properties:
        hour:
          type: integer
          minimum: 0
          maximum: 23
        minute:
          type: integer
          minimum: 0
          maximum: 59
    UserSettings:
      type: object
      required:
        - morning_review_at
        - evening_review_at
        - weekly_review_at
        - monthly_review_at
        - quiet_hours_start
        - quiet_hours_end
        - language
      properties:
        morning_review_at:
          $ref: '#/components/schemas/TimeOfDay'
        evening_review_at:
          $ref: '#/components/schemas/TimeOfDay'
        weekly_review_at:
          $ref: '#/components/schemas/TimeOfDay'
        monthly_review_at:
          $ref: '#/components/schemas/TimeOfDay'
        quiet_hours_start:
          oneOf:
            - $ref: '#/components/schemas/TimeOfDay'
            - type: 'null'
        quiet_hours_end:
          oneOf:
            - $ref: '#/components/schemas/TimeOfDay'
            - type: 'null'
        language:
          type: string
          example: ru
    AnalyticsSummary:
      type: object
      required:
        - period_label
        - tasks_created
        - tasks_completed
        - completion_rate
        - open_tasks
        - habit_consistency
        - habit_completions
        - habit_count
        - projects
      properties:
        period_label:
          type: string
        tasks_created:
          type: integer
          format: int64
        tasks_completed:
          type: integer
          format: int64
        completion_rate:
          type: integer
          minimum: 0
          maximum: 100
          description: Integer percent 0–100
        open_tasks:
          type: integer
          format: int64
        habit_consistency:
          type: integer
          minimum: 0
          maximum: 100
          description: Integer percent 0–100
        habit_completions:
          type: integer
          format: int64
        habit_count:
          type: integer
          format: int64
        projects:
          type: array
          items:
            $ref: '#/components/schemas/AnalyticsProjectKPI'
    AnalyticsProjectKPI:
      type: object
      required: [title, percent]
      properties:
        title:
          type: string
        percent:
          type: string
          description: Progress label (e.g. "42%" or "—")
    Note:
      type: object
      required: [id, body, tags, created_at]
      properties:
        id:
          type: string
          format: uuid
        body:
          type: string
        tags:
          type: array
          items:
            type: string
        created_at:
          type: string
          format: date-time
    Debt:
      type: object
      required: [id, creditor, amount_cents, paid_cents, remaining_cents, currency]
      properties:
        id:
          type: string
          format: uuid
        creditor:
          type: string
        amount_cents:
          type: integer
          format: int64
        paid_cents:
          type: integer
          format: int64
        remaining_cents:
          type: integer
          format: int64
        currency:
          type: string
          example: RUB
        due_date:
          type: string
          format: date
    Reminder:
      type: object
      required: [id, message, fire_at, status]
      properties:
        id:
          type: string
          format: uuid
        message:
          type: string
        fire_at:
          type: string
          format: date-time
        status:
          type: string
    Contact:
      type: object
      required: [id, name, company, role, notes, created_at]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        company:
          type: string
        role:
          type: string
        notes:
          type: string
        created_at:
          type: string
          format: date-time
    Skill:
      type: object
      required: [id, name, level, created_at]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        level:
          type: string
        created_at:
          type: string
          format: date-time
    WeightLog:
      type: object
      required: [id, weight_kg, logged_at]
      properties:
        id:
          type: string
          format: uuid
        weight_kg:
          type: number
          format: double
        logged_at:
          type: string
          format: date-time
    StepLog:
      type: object
      required: [id, steps, logged_at]
      properties:
        id:
          type: string
          format: uuid
        steps:
          type: integer
        logged_at:
          type: string
          format: date-time
    SleepLog:
      type: object
      required: [id, duration_minutes, duration_hours, logged_at]
      properties:
        id:
          type: string
          format: uuid
        duration_minutes:
          type: integer
        duration_hours:
          type: number
          format: double
        logged_at:
          type: string
          format: date-time
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
